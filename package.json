{
  "name": "renweb-engine-repo",
  "private": true,
  "workspaces": [
    "web/api",
    "cli"
  ],
  "scripts": {
    "build:api": "npm run build --workspace=web/api",
    "build:cli": "npm run build --workspace=cli"
  }
}
